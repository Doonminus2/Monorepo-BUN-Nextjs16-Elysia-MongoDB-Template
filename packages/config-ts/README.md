
---

# 📄 `packages/config-ts/README.md`

### แนวคิด

ใช้เป็น baseline กลาง

ให้แต่ละแอป override เฉพาะสิ่งที่จำเป็น

ไม่ผูกกับ framework ใด framework หนึ่ง

```md
# TypeScript Base Configuration

Package นี้ใช้เก็บ TypeScript base configuration
เพื่อให้ทุกแอปใน Monorepo ใช้มาตรฐานเดียวกัน

## การใช้งาน

ในแต่ละแอปให้ extends tsconfig นี้

```json
{
  "extends": "../../packages/config-ts/tsconfig.json"
}
